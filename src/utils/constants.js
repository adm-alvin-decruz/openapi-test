const GROUP = {
    WILD_PASS: 'wildpass',
    FOW: 'fow',
    FOW_PLUS: 'fow+'
}

const LANGUAGE_CODE = {
    JAPAN: 'ja',
    ENGLISH: 'en',
    KOREAN: 'kr',
    CHINA: 'zh'
}

const GROUPS_SUPPORTS = [GROUP.WILD_PASS, GROUP.FOW, GROUP.FOW_PLUS];

module.exports = {
    GROUP,
    LANGUAGE_CODE,
    GROUPS_SUPPORTS
}
